# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
*.tsbuildinfo
.tscache/

# Test outputs
coverage/
.nyc_output/

# TypeDoc output (gh-pages publishes it; never tracked in main)
docs/

# npm pack tarballs (created by `npm pack`, consumed by example-smoke CI)
*.tgz

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo
*~

# Local env
.env
.env.local
.env.*.local

# Logs
*.log

# Lockfiles — keep them so installs are reproducible, do not gitignore.
# (package-lock.json, yarn.lock, pnpm-lock.yaml stay tracked)
